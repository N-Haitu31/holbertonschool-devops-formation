services:
  db:
    image: postgres:16
    environment:
      - POSTGRES_PASSWORD=toto123

  api:
    build: ./api
    ports:
      - "5000:5000"
  
  web:
    image: nginx:alpine
    ports:
      - "8090:80"