# Running bash check-setup.sh:

```
=== Vérification de l'environnement ===
  [ OK ] Docker installé et démon actif (Docker version 29.6.2)
  [ OK ] Git installé (git version 2.34.1)
  [ OK ] git user.name configuré
  [ OK ] git user.email configuré
  [ OK ] Clé SSH GitHub fonctionnelle
  [ OK ] Node.js présent (v20.20.2)
=======================================
Tout est pret. Bon cours !
```

# docker run hello-world:

Running docker run hello-world printed the 'Hello from Docker!' message, confirming the Docker client can successfully communicate with the daemon.
